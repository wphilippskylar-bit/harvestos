// Demo mode lets the app run — and be screenshotted, previewed, and clicked through —
// before a real Supabase project is wired up. Toggle off once you've connected your own
// database (set NEXT_PUBLIC_DEMO_MODE=false in .env.local).
export const DEMO_MODE = process.env.NEXT_PUBLIC_DEMO_MODE === "true";
